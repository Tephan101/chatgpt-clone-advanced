# Copy this file to .env and fill in your API key.
OPENAI_API_KEY=
# Optionally specify a different model (default uses gpt-4.1-mini if not set).
# OPENAI_MODEL=gpt-5
# Optionally specify a different OpenAI API base (e.g., for Azure or proxies)
# OPENAI_API_BASE=https://api.openai.com/v1
